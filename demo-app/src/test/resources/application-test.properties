# Test configuration
spring.application.name=demo-app-test
server.port=0

# Disable Kafka auto-configuration in tests unless explicitly enabled
spring.kafka.bootstrap-servers=${kafka.bootstrap-servers:localhost:9092}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.group-id=test-consumer-group

# Logging configuration for tests
logging.level.root=INFO
logging.level.dev.surovtsev.demo.app=DEBUG
logging.level.org.springframework.test=DEBUG